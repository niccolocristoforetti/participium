$kind: http-example
name: Operator or admin role required.
request:
  url: "{{baseUrl}}/operator/reports/:report_id/assign"
  method: POST
  headers:
    Accept: application/json
  pathVariables:
    report_id: "6011"
response:
  statusCode: 403
  statusText: Forbidden
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "error": "string"
      }
order: 3000
