$kind: http-example
name: Authentication required.
request:
  url: "{{baseUrl}}/reports/:report_id/messages"
  method: POST
  headers:
    Content-Type: application/json
    Accept: application/json
  pathVariables:
    report_id: "6011"
  body:
    type: json
    content: |-
      {
        "body": "string"
      }
response:
  statusCode: 401
  statusText: Unauthorized
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "error": "string"
      }
order: 3000
